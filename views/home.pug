extends layouts/main
include mixins/videoBlock

block content
   .videos 
        each video in dbvideos 
            +videoBlock({
                title:video.title,
                views:video.views,
                videofile:video.videofile
            })